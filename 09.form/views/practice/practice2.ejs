<!DOCTYPE html>
<html lang="ko">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>실습문제2</title>
    <style>
      fieldset {
        width: 600px;
      }
    </style>
  </head>
  <body>
    <h1>실습문제2</h1>
    <form
      action="/practice2-post"
      method="POST"
      name="practice2"
      target="_blank"
      onsubmit="return submitForm(this)"
    >
      <label>이름 <input type="text" name="user" /></label>
      <br />
      <br />
      <fieldset>
        <legend>성별</legend>
        <label><input type="radio" name="gender" value="남자" />남자</label>
        <label><input type="radio" name="gender" value="여자" />여자</label>
      </fieldset>
      <br />
      <fieldset>
        <legend>생년월일</legend>
        <label>
          <select name="year" id="year">
            <%for(i=1970; i<=2024; i++){%>
            <option value="<%=i%>"><%=i%></option>
            <%}%>
          </select>
          년</label
        ><label>
          <select name="month" id="month">
            <%for(i=1; i<=12; i++){%>
            <option value="<%=i%>"><%=i%></option>
            <%}%>
          </select>
          월</label
        >
        <label>
          <select name="date" id="date">
            <%for(i=1; i<=31; i++){%>
            <option value="<%=i%>"><%=i%></option>
            <%}%>
          </select>
          일</label
        >
      </fieldset>
      <br />
      <fieldset>
        <legend>관심사</legend>
        <label>
          <input type="checkbox" name="interest" value="여행" />
          여행
        </label>
        <label>
          <input type="checkbox" name="interest" value="패션" />
          패션
        </label>
        <label>
          <input type="checkbox" name="interest" value="음식" />
          음식
        </label>
      </fieldset>
      <br />
      <fieldset>
        <legend>가입 정보</legend>
        <label>
          ID
          <input
            type="text"
            name="id"
            pattern="^([a-z0-9]){8,12}$"
            placeholder="ID를 입력해주세요"
            title="영어 소문자와 숫자를 조합해 8~12자로 입력해주세요"
          />
        </label>
        <label>
          Pw
          <input
            type="password"
            name="pw"
            pattern="^(?=.*[A-Z])(?=.*[\W])(?=.*[a-z])(?=.*[0-9]){8,}$"
            placeholder="password"
            title="영어 대문자, 소문자, 특수기호, 숫자를 1개 이상씩 포함해 8자 이상으로 입력해주세요 "
          />
        </label>
      </fieldset>
      <fieldset>
        <legend>MBTI</legend>
        <select name="EI">
          <option value="E">E</option>
          <option value="I">I</option>
        </select>
        <select name="SN">
          <option value="S">S</option>
          <option value="N">N</option>
        </select>
        <select name="FT">
          <option value="F">F</option>
          <option value="T">T</option>
        </select>
        <select name="PJ">
          <option value="P">P</option>
          <option value="J">J</option>
        </select>
      </fieldset>
      <br />

      <button>회원가입</button>
    </form>
    <script>
      function submitForm(form) {
        if (form.user.value.trim() === "") {
          alert("이름을 입력해주세요!");
          return false;
        }
        if (!form.gender[0].checked && !form.gender[1].checked) {
          alert("성별을 체크해주세요!");
          return false;
        }
      }
    </script>
  </body>
</html>
